name: Release

on:
  push:
    tags:
      - "v*-stable*"
      - "v*-preview*"
      - "v*-standalone*"

# This workflow will trigger on each push to the `release` branch to create or update a GitHub release, build your app, and upload the artifacts to the release.

permissions:
  contents: read

# Never cancel a release in flight; this only stops the same tag from being
# built twice concurrently (e.g. a re-run overlapping the original).
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  # Thin LTO + parallel codegen build far faster than fat LTO with a single
  # codegen unit, at a negligible runtime cost for this I/O-bound app. Applied
  # to every CI build path (see validate-desktop.yml and msstore.yml) so release
  # artifacts are produced with the same profile they were validated with.
  # Cargo.toml is unchanged, so local `bun run build:tauri` still uses fat LTO.
  CARGO_PROFILE_RELEASE_LTO: thin
  CARGO_PROFILE_RELEASE_CODEGEN_UNITS: "16"
  CARGO_INCREMENTAL: "0"

jobs:
  ensure-release:
    permissions:
      contents: write
    runs-on: ubuntu-24.04
    steps:
      - name: Ensure GitHub release exists
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ github.ref_name }}
          REPO: ${{ github.repository }}
        shell: bash
        run: |
          if gh release view "$TAG_NAME" --repo "$REPO" >/dev/null 2>&1; then
            exit 0
          fi

          version="${TAG_NAME#v}"
          prerelease_flag=()
          if [[ "$TAG_NAME" != *stable ]]; then
            prerelease_flag+=(--prerelease)
          fi

          gh release create "$TAG_NAME" \
            --repo "$REPO" \
            --title "Quadrant v${version}" \
            --notes "See the assets to download this version and install. Check out the [changelog on my blog](https://blog.mrquantumoff.dev)" \
            "${prerelease_flag[@]}"

  publish-tauri:
    needs: ensure-release
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "blacksmith-4vcpu-ubuntu-2404"
            args: ""
          - platform: "blacksmith-4vcpu-windows-2025"
            args: ""
          - platform: "blacksmith-4vcpu-ubuntu-2404-arm"
            args: ""
          - platform: "windows-11-arm"
            args: ""
          - platform: "macos-latest"
            args: "--target aarch64-apple-darwin"

    runs-on: ${{ matrix.platform }}
    outputs:
      assets: ${{steps.tauri.outputs.releaseUploadUrl}}
      version: ${{steps.tauri.outputs.appVersion}}
    steps:
      - uses: actions/checkout@v6

      - name: Setup bun
        uses: oven-sh/setup-bun@v2

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ contains(matrix.platform, 'macos') && 'aarch64-apple-darwin' || '' }}

      - name: install dependencies (ubuntu only)
        if: contains(matrix.platform, 'ubuntu')
        run: |
          sudo apt update
          sudo apt install -y libwebkit2gtk-4.1-dev xdg-utils libappindicator3-dev librsvg2-dev patchelf libsecret-1-dev

      - name: install frontend dependencies (bun)
        run: bun install

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - uses: tauri-apps/tauri-action@v0
        id: tauri
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          ETERNAL_API_TOKEN: ${{ secrets.ETERNAL_API_TOKEN }}
          QUADRANT_API_KEY: ${{ secrets.QUADRANT_API_KEY }}
          QUADRANT_OAUTH2_CLIENT_ID: ${{ secrets.QUADRANT_OAUTH2_CLIENT_ID }}
          QUADRANT_OAUTH2_CLIENT_SECRET: ${{ secrets.QUADRANT_OAUTH2_CLIENT_SECRET }}
          APPLE_SIGNING_IDENTITY: ${{ contains(matrix.platform, 'macos') && '-' || '' }}
          NO_STRIP: ${{ contains(matrix.platform, 'ubuntu') && 'true' }}
        with:
          releaseName: "Quadrant v__VERSION__"
          releaseBody: "See the assets to download this version and install. Check out the [changelog on my blog](https://blog.mrquantumoff.dev)"
          releaseDraft: false
          prerelease: ${{ !endsWith(github.ref_name, 'stable') }}
          args: ${{ matrix.args }}
          tauriScript: "bun tauri"
          tagName: ${{github.ref_name}}

  sync_flathub:
    name: Sync Flathub
    needs: publish-tauri
    if: endsWith(github.ref_name, '-stable') || endsWith(github.ref_name, '-flatpak')
    uses: ./.github/workflows/flatpak.yml
    secrets: inherit
    with:
      tag: ${{ github.ref_name }}

  publish_aur:
    name: Publish to AUR
    needs: publish-tauri
    if: endsWith(github.ref_name, '-stable')
    uses: ./.github/workflows/aur.yml
    secrets: inherit
    with:
      tag: ${{ github.ref_name }}

  publish_winget:
    name: Publish to WinGet
    needs: publish-tauri
    if: endsWith(github.ref_name, '-stable')
    uses: ./.github/workflows/winget.yml
    secrets: inherit
    with:
      tag: ${{ github.ref_name }}

  submit_update:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    needs: publish-tauri
    if: endsWith(github.ref_name, '-flatpak') == false && endsWith(github.ref_name, '-msstore') == false
    steps:
      - name: Parse semver string
        id: semver_parser
        uses: booxmedialtd/ws-action-parse-semver@v1
        with:
          input_string: ${{github.ref_name}}
      - uses: winterjung/split@v2
        id: split
        with:
          msg: ${{steps.semver_parser.outputs.prerelease}}
          separator: "."
      - env:
          VERSION: ${{github.ref_name}}
          CHANNEL: ${{steps.split.outputs._0}}
        run: |
          curl -X PUT --header 'Authorization: ${{secrets.VERSION_UPDATE_TRIGGER_TOKEN}}' https://api.usequadrant.dev/api/any/quadrant/add_updates/$CHANNEL/$VERSION
